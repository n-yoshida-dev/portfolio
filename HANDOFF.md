# 引き継ぎ

`/apps-workflow:handoff` で更新する。**次のセッションが最初に読むファイル。**

**ここには「今どこにいるか」だけを書く。目安 40 行。履歴を積まない。**

| 書きたくなったこと | 行き先 |
|---|---|
| 決めたこと・理由・却下した案 | `logs/decisions.md`（`/decide` で追記） |
| ハマったこと・落とし穴・技術的な選択の理由 | `KNOWLEDGE.md` |
| やること・ユーザーへの確認待ち | `TODO.md` |
| やったこと | 書かない。`git log` と PR が持っている |
| 毎回守る手順・環境の注意 | `CLAUDE.md`（短く）か `.claude/rules/` |

## 進捗

```
進捗（TODO.md より自動集計・2026-09-26）

█████████████████░░░  88%  残り  1 /  9  フェーズ1：Phase 1（公開できる状態まで）
█████████████████░░░  88%  残り  1 /  9  合計
あなたの回答待ち：0 件（回答済み 3 件）
保留（合計に含めない）：6 件（済み 0 件）

前回の区切り（2026-09-26）から：完了 +1 件、新たに見つかったタスク +1 件
```

## 現在地

- フェーズ：Phase 1 は残り 1 タスク（Ask AI の日本語回答の再確認）。公開済み。確認待ちは 0 件
- main：クリーン。作業中のブランチは無い
- Vercel：接続済み。本番 URL は `README.md` 冒頭。`/llms.txt`・sitemap・Ask AI の実地確認（Claude、区分の混同なし）まで済み
- Ask AI の回答が英語だったのでプロンプトに `Answer in Japanese.` を足した。**本番で日本語になるかの再確認はユーザー操作**（`TODO.md` 先頭）

## 次セッションで最初にやること

1. ユーザーに本番の `/ask`（URL は `README.md` 冒頭 + `/ask`）を Claude で試し直してもらい、日本語で返れば `TODO.md` 先頭を完了にする

`TODO.md` の先頭と一致させる。書くのは最初の一手だけで、一覧は `TODO.md` が持つ。
