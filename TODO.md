# TODO

SessionStart フックが `##` 見出しごとに `- [x]` / `- [ ]` を数えて進捗表を出し、`- [ ]` の行を先頭12件まで提示する。**この記法を崩さないこと。**
着手順に並べる。上ほど先にやる。

**`##` 見出しが進捗表の 1 行（フェーズ）になる。** 見出しに「確認待ち」を含む節はユーザーの回答待ち、「保留」を含む節は急がない持ち越しとして、
どちらも合計から外れて別枠に出る。急がないタスクを合計に混ぜたくなければ、見出しに「（保留）」を付ける。
**1 タスクは PR 1 本の大きさに揃える。** 件数が進捗の単位になるため、大きすぎるタスクはバーを止め、小さすぎるタスクは水増しになる。

**タスクには「完了条件：」の行を付ける。** 次の行に半角スペース 2 つで字下げして書く（チェックボックスではないので、進捗の件数にも未完一覧にも入らない）。
完了条件は、コードを読まずに確かめられる言葉で書く。「何を操作したら、何が見えれば完了か」。
CI（整形・型・テスト・ビルド）は「動くが意図と違う」を止められない。この行がその代わりになる。
PR を作るときは、この行を PR 本文の「完了条件」に引き、実際に確かめた証拠を 1 行添える（`../CLAUDE.md`「Git運用ルール」）。
完了条件が書けないタスクは、まだ着手できる大きさになっていない。分ける。

> - [ ] 年収の入力欄を変えると総資産グラフが再計算される
>   完了条件：ブラウザで年収を 500 万から 600 万に変えると、グラフ最終年の総資産が増える

## フェーズ1：設計

- [ ] `PLAN.md` を書く
  完了条件：「やらないこと」が 1 つ以上書いてある
- [ ] `SPEC.md` の初版を書く
  完了条件：フェーズ1 で作る範囲のデータモデルと画面が「未定」でなくなっている

## フェーズ2：実装

（未定。タスクごとに「完了条件：」を付ける）

## 確認待ち（ユーザーの回答が要るもの）

（なし。`- [ ]` で書けば起動時に表示される。回答をもらったらチェックを入れ、決まったことは `logs/decisions.md` へ）
